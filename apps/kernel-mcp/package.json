{
  "name": "@kingly/kernel-mcp",
  "version": "0.0.1",
  "description": "Kernel MCP - The heart of Kingly OS",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "go run .",
    "build": "go build -o dist/kernel-mcp",
    "test": "go test ./...",
    "bench": "go test -bench=. ./..."
  },
  "dependencies": {
    "@kingly/protocol": "workspace:*"
  }
}