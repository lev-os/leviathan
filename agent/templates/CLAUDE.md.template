# {{PROJECT_NAME}} - AI Intelligence Configuration

## Workspace Intelligence Level: {{INTELLIGENCE_LEVEL}}
{{#if PARENT_WORKSPACE}}
**Parent System:** {{PARENT_WORKSPACE}}
**Intelligence Inheritance:** {{INTELLIGENCE_INHERITANCE}}
{{/if}}

## Session Commands

### Auto-Ping Triggers
{{AUTO_PING_TRIGGERS}}

### Cross-Workspace Routing  
{{CROSS_WORKSPACE_ROUTES}}

### Session Management
```bash
# Create session checkpoint
kingly-semantic ping --context="{{DEFAULT_CONTEXT}}" --significance={{DEFAULT_SIGNIFICANCE}}

# Prepare session handoff
kingly-semantic handoff --include="{{HANDOFF_INCLUDES}}" --format="package"

# Load previous session context
kingly-semantic load --latest --restore-level="{{RESTORE_LEVEL}}"
```

## Intelligence Access

### Slash Commands
```
/think    → kingly-semantic power --context="current" --auto-save=true
/all      → kingly-semantic lookup --all --scope="{{INTELLIGENCE_SCOPE}}"
/kingly   → kingly-semantic workflow --interactive --context="current"
/exec     → kingly-semantic execute --chain --progress=true
/ping     → kingly-semantic ping --auto --significance="auto-score"
/handoff  → kingly-semantic handoff --prepare --format="package"
/load     → kingly-semantic load --latest --validate=true
/sync     → kingly-semantic template sync --all --strategy="intelligent-merge"
/evolve   → kingly-semantic template evolve --context="current" --preview=true
```

### Intelligence Patterns
{{INTELLIGENCE_PATTERNS}}

### Context Propagation
{{CONTEXT_PROPAGATION_RULES}}

## Template System

### Template Inheritance
- **Global Templates:** `~/.kingly/templates/`
- **Local Overrides:** `$PWD/.kingly/templates/`
- **Evolution Tracking:** Auto-evolution based on usage patterns

### Template Commands
```bash
# Initialize workspace with AI-first templates
kingly-semantic template init --workspace-type="{{WORKSPACE_TYPE}}" --intelligence-level="{{INTELLIGENCE_LEVEL}}"

# Synchronize templates across workspaces
kingly-semantic template sync --target-workspace="{{SYNC_TARGET}}" --strategy="{{MERGE_STRATEGY}}"

# Evolve templates based on usage patterns
kingly-semantic template evolve --usage-data="{{USAGE_PERIOD}}" --evolution-mode="{{EVOLUTION_MODE}}"
```

## Distributed Intelligence Network

### Network Configuration
- **Intelligence Hub:** `~/.kingly/intelligence/`
- **Local Cache:** `$PWD/.kingly/intelligence/cache/`
- **Privacy Level:** {{PRIVACY_LEVEL}}
- **Contribution Mode:** {{CONTRIBUTION_MODE}}

### Intelligence Commands
```bash
# Network status and health
kingly-semantic intelligence status --workspace={{WORKSPACE_NAME}}

# Contribute breakthrough insights
kingly-semantic intelligence contribute --context="{{CONTRIBUTION_CONTEXT}}" --significance={{CONTRIBUTION_SIGNIFICANCE}}

# Query cross-workspace intelligence
kingly-semantic intelligence query --pattern="{{QUERY_PATTERN}}" --scope="{{QUERY_SCOPE}}"

# Network optimization
kingly-semantic network optimize --target="{{OPTIMIZATION_TARGET}}" --preserve-privacy=true
```

## Privacy & Security

### Workspace Isolation
- **Isolation Level:** {{ISOLATION_LEVEL}}
- **Contribution Filtering:** {{CONTRIBUTION_FILTERING}}
- **Sanitization Rules:** {{SANITIZATION_RULES}}

### Security Settings
```yaml
privacy_settings:
  isolation_level: {{ISOLATION_LEVEL}}
  contribution_filtering: {{CONTRIBUTION_FILTERING}}
  pii_detection: {{PII_DETECTION}}
  credential_scrubbing: {{CREDENTIAL_SCRUBBING}}
  custom_patterns: {{CUSTOM_PATTERNS}}
```

## Development Workflow

### LLM-First Principles
{{LLM_FIRST_PRINCIPLES}}

### Implementation Checklist
{{IMPLEMENTATION_CHECKLIST}}

### BDD Testing Approach
{{BDD_TESTING_APPROACH}}

---
*Generated by Kingly Template System v{{TEMPLATE_VERSION}}*  
*Last Evolution: {{LAST_EVOLUTION_DATE}}*  
*Intelligence Level: {{INTELLIGENCE_LEVEL}}*  
*Template Effectiveness: {{TEMPLATE_EFFECTIVENESS}}%*